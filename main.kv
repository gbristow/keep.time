#:import MainScreen app.main_screen
#:import EventTimer app.event_timer
#:import TimerBar app.timer_bar

BoxLayout:
    ScreenManager:
        id: manager

        MainScreen:
            name: "main"
            id: main

        #EventTimer:
        #    name: "timer"
        #    id: timer